name: 📖 Docs

on:
  push:
    branches:
      - main
      - docs
  workflow_dispatch:

jobs:
  build:
    name: 📓 Build and deploy docs
    uses: WGBH-MLA/.github/.github/workflows/docs.yml@main
